services:
  postgres:
    image: postgres:16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: strava_user
      POSTGRES_PASSWORD: localdev123
      POSTGRES_DB: strava_routes
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: